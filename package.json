{
	"private": true,
	"devDependencies": {
		"pyright": "^1.1.281"
	},
	"packageManager": "pnpm@7.17.0",
	"scripts": {
		"postinstall": "poetry install",
		"format": "poetry run black asix",
		"check": "pyright asix",
		"all": "pnpm run format && pnpm run check"
	}
}
